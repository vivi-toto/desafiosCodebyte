const myString = "MrtyNNgMM";
const caracterToFind = "M";

if (myString.includes(caracterToFind)) {
    console.log(`A string contém o caracter que você procura: ${caracterToFind}`);
    
} else {
    console.log(`A string não contém o caracter que você procura: ${caracterToFind}`);
}
